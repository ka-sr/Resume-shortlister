PORT=4000
CLIENT_ORIGIN=http://127.0.0.1:5173

# Optional. If omitted, the app stores analysis runs in data/submissions.json.
# DATABASE_URL=postgres://user:password@localhost:5432/resume_shortlister
